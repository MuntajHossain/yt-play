use caveman full
i want you to changes few things. 
  1. when open show menu (play history, search). from play history i should be able to play
  the audio. Audio is playable if it is present else download.
  2. If search selected search screen is shown. previous search is shown below(max 10). if i give $0 then first item from search list is selected and result is fetched again.

